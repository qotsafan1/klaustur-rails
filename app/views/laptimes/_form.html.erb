<%= form_for(laptime) do |f| %>
  <% if laptime.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(laptime.errors.count, "error") %> prohibited this laptime from being saved:</h2>

      <ul>
      <% laptime.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :competitor_id %>
    <%= f.text_field :competitor_id %>
  </div>

  <div class="field">
    <%= f.label :lap_number %>
    <%= f.number_field :lap_number %>
  </div>

  <div class="field">
    <%= f.label :laptime %>
    <%= f.text_area :laptime %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
